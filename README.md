# Express api

```
yarn add express
yarn add cors morgan
```

### ESLint

```
yarn add -D eslint
# JavaScript Standard Style
yarn add -D eslint-config-standard eslint-plugin-standard eslint-plugin-promise eslint-plugin-import eslint-plugin-node
# create rc & test
node_modules\.bin\eslint --init
```
